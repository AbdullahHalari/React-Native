import React from "react";
import { Text,StyleSheet } from "react-native";

const CustomApp = ()=>{
  return( 
  <Text style={styles.textStyle}>hobbfgbw are you!</Text>
  );
};

const styles =StyleSheet.create({
  textStyle: {
    color:"black"
  }
});

export default CustomApp